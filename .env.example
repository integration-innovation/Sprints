# Secret used to sign participant session cookies. Set this in production.
SPRINTS_SESSION_SECRET=change-me-to-a-long-random-string

# Optional: path to the SQLite database file (default: ./data/sprints.db)
# SPRINTS_DB_PATH=./data/sprints.db
